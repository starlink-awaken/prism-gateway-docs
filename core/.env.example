# PRISM-Gateway 环境变量配置示例
#
# 复制此文件为 .env 并根据实际环境修改配置值
# 注意：.env 文件包含敏感信息，不要提交到版本控制系统

# ==========================================
# 环境配置
# ==========================================

# 运行环境：development | production | test
NODE_ENV=development

# API 服务配置
PORT=3000
HOSTNAME=0.0.0.0

# ==========================================
# CORS 安全配置（重要）
# ==========================================

# 允许的跨域来源（逗号分隔）
#
# 安全要求：
# 1. 必须包含完整 URL（协议 + 域名 + 端口）
# 2. 不要使用通配符 (*)
# 3. 生产环境必须明确配置
# 4. 不要包含多余的空格
#
# 开发环境示例：
# CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173
#
# 生产环境示例：
# CORS_ALLOWED_ORIGINS=https://example.com,https://app.example.com
#
# 留空时：
# - 开发环境：自动允许 localhost 和 127.0.0.1
# - 生产环境：拒绝所有跨域请求（安全默认）
CORS_ALLOWED_ORIGINS=

# ==========================================
# JWT 认证配置
# ==========================================

# JWT 密钥（至少 32 字符，生产环境必须使用强随机密钥）
JWT_SECRET=your-super-secret-jwt-key-at-least-32-characters-long

# JWT 访问令牌有效期（秒）
# 默认 3600（1 小时）
JWT_ACCESS_TTL=3600

# JWT 刷新令牌有效期（秒）
# 默认 604800（7 天）
JWT_REFRESH_TTL=604800

# JWT 签发者标识
JWT_ISSUER=prism-gateway

# JWT 目标受众
JWT_AUDIENCE=prism-gateway-api

# ==========================================
# 数据存储配置
# ==========================================

# MEMORY 数据根目录
PRISM_DATA_ROOT=~/.prism-gateway

# ==========================================
# 日志配置
# ==========================================

# 日志级别：debug | info | warn | error
LOG_LEVEL=info

# 日志格式：json | text
LOG_FORMAT=json

# ==========================================
# 速率限制配置
# ==========================================

# 最大并发请求数
RATE_LIMIT_MAX_CONCURRENT=10

# 请求超时时间（毫秒）
RATE_LIMIT_TIMEOUT=5000

# 队列最大长度
RATE_LIMIT_QUEUE_LIMIT=20

# ==========================================
# Analytics 配置
# ==========================================

# 缓存最大条目数
ANALYTICS_CACHE_MAX_SIZE=100

# 缓存默认 TTL（秒）
ANALYTICS_CACHE_DEFAULT_TTL=300

# ==========================================
# MCP Server 配置
# ==========================================

# MCP 服务名称
MCP_SERVER_NAME=prism-gateway

# MCP 服务版本
MCP_SERVER_VERSION=2.0.0
