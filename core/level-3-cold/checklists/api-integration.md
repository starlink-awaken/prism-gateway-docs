# API 集成检查清单

> 前后端 API 集成测试和验证标准清单

**版本：** 1.0.0
**适用场景：** 前端与后端 API 集成开发和测试
**预计时间：** 2-4 小时
**最后更新：** 2026-02-07

---

## 📋 使用说明

### 何时使用此清单
- [ ] 实现新的 API 端点集成
- [ ] 修改现有 API 调用逻辑
- [ ] 调试前后端集成问题
- [ ] 进行集成测试验证

### 使用步骤
1. 准备后端 API 环境
2. 按清单逐项检查
3. 记录问题和解决方案
4. 完成后标记所有项

---

## 🚀 准备阶段 (Preparation)

### 后端 API 环境
- [ ] **后端服务器运行正常**
  ```bash
  cd prism-gateway
  bun run api
  # 验证：http://localhost:3000/health
  ```

- [ ] **API 版本匹配**
  - 前端期望版本：`[版本号，如 v1]`
  - 后端提供版本：`[验证版本]`
  - 版本兼容：✅ / ❌

- [ ] **测试数据准备**
  - 创建测试用户账号
  - 生成测试数据（检查记录、违规记录等）
  - 准备边界条件数据（空数据、大量数据）

### 前端开发环境
- [ ] **前端服务器运行正常**
  ```bash
  cd web-ui
  npm run dev
  # 访问：http://localhost:5173
  ```

- [ ] **Vite 代理配置正确**
  ```typescript
  // vite.config.ts
  proxy: {
    '/api': {
      target: 'http://localhost:3000',
      changeOrigin: true
    },
    '/ws': {
      target: 'ws://localhost:3000',
      ws: true
    }
  }
  ```
  - API 代理：✅ / ❌
  - WebSocket 代理：✅ / ❌

- [ ] **环境变量配置**
  ```bash
  # .env.local（如需要）
  VITE_API_BASE_URL=http://localhost:3000/api/v1
  VITE_WS_URL=ws://localhost:3000/ws
  ```

---

## 🔌 API 端点测试 (API Endpoints)

### 健康检查端点
- [ ] **GET /health**
  ```bash
  curl http://localhost:3000/health
  # 期望：200 OK, { "status": "ok" }
  ```

- [ ] **前端能够访问**
  ```javascript
  const response = await fetch('http://localhost:3000/health');
  const data = await response.json();
  console.log(data); // { status: 'ok' }
  ```

### Analytics API 端点

#### Dashboard API
- [ ] **GET /api/v1/analytics/dashboard**
  ```bash
  # 测试各个周期
  curl "http://localhost:3000/api/v1/analytics/dashboard?period=today"
  curl "http://localhost:3000/api/v1/analytics/dashboard?period=week"
  curl "http://localhost:3000/api/v1/analytics/dashboard?period=month"
  curl "http://localhost:3000/api/v1/analytics/dashboard?period=year"
  ```

  **验证响应格式：**
  ```json
  {
    "success": true,
    "data": {
      "summary": {
        "totalChecks": number,
        "totalViolations": number,
        "todayRetros": number
      },
      "quality": { ... },
      "performance": { ... },
      "trends": { ... },
      "alerts": [ ... ]
    },
    "meta": {
      "timestamp": "ISO 8601 string"
    }
  }
  ```

  **检查项：**
  - [ ] 响应状态码：200
  - [ ] 响应格式符合 `ApiResponse<Dashboard>` 类型
  - [ ] 数据完整性：所有必填字段存在
  - [ ] 数据准确性：数值合理（无负数、无 NaN）

#### Usage Metrics API
- [ ] **GET /api/v1/analytics/usage**
  ```bash
  curl "http://localhost:3000/api/v1/analytics/usage?period=week"
  ```

  **验证：**
  - [ ] 响应状态码：200
  - [ ] 包含 `totalChecks`, `activeUsers`, `avgSessionTime` 等字段

#### Quality Metrics API
- [ ] **GET /api/v1/analytics/quality**
  ```bash
  curl "http://localhost:3000/api/v1/analytics/quality?period=month"
  ```

  **验证：**
  - [ ] 响应状态码：200
  - [ ] 包含 `violationRate`, `topViolatedPrinciples` 等字段

#### Performance Metrics API
- [ ] **GET /api/v1/analytics/performance**
  ```bash
  curl "http://localhost:3000/api/v1/analytics/performance?period=year"
  ```

  **验证：**
  - [ ] 响应状态码：200
  - [ ] 包含 `avgCheckTime`, `p50`, `p95`, `p99` 等字段

### Authentication API（如已实现）
- [ ] **POST /api/v1/auth/login**
  ```bash
  curl -X POST http://localhost:3000/api/v1/auth/login \
    -H "Content-Type: application/json" \
    -d '{"username":"testuser","password":"password123"}'
  ```

  **验证：**
  - [ ] 响应状态码：200
  - [ ] 返回 JWT token
  - [ ] Token 可用于后续请求

---

## 🌐 前端 API 集成 (Frontend Integration)

### APIService 实现
- [ ] **API Service 类正确实现**
  ```typescript
  // src/services/api.ts
  class APIService {
    private baseUrl = '/api/v1';

    async getDashboard(period: Period): Promise<Dashboard> {
      const response = await fetch(`${this.baseUrl}/analytics/dashboard?period=${period}`);
      if (!response.ok) throw new Error(`API error: ${response.status}`);
      const json: ApiResponse<Dashboard> = await response.json();
      return json.data;
    }
  }
  ```

- [ ] **错误处理完善**
  - [ ] 网络错误捕获
  - [ ] HTTP 状态码错误处理（4xx, 5xx）
  - [ ] 超时处理（可选）
  - [ ] 错误信息用户友好

- [ ] **类型安全**
  - [ ] 所有响应都有 TypeScript 类型定义
  - [ ] 使用 `ApiResponse<T>` 包装器类型
  - [ ] 无 `any` 类型使用

### Store 集成
- [ ] **Zustand Store 正确调用 API**
  ```typescript
  // src/stores/analyticsStore.ts
  const useAnalyticsStore = create<AnalyticsState>((set) => ({
    dashboard: null,
    loading: false,
    error: null,

    fetchDashboard: async (period) => {
      set({ loading: true, error: null });
      try {
        const dashboard = await apiService.getDashboard(period);
        set({ dashboard, loading: false });
      } catch (error) {
        set({ error: error.message, loading: false });
      }
    }
  }));
  ```

- [ ] **状态管理正确**
  - [ ] loading 状态在请求前设置为 true
  - [ ] loading 状态在请求后设置为 false（成功/失败）
  - [ ] error 状态在请求前清除
  - [ ] error 状态在失败时设置

### 组件集成
- [ ] **Dashboard 页面正常加载数据**
  ```typescript
  // src/pages/Dashboard.tsx
  useEffect(() => {
    fetchDashboard();
  }, [fetchDashboard]);
  ```

  **验证：**
  - [ ] 页面首次加载时自动调用 API
  - [ ] 显示 loading 状态
  - [ ] 数据加载成功后正常渲染
  - [ ] 数据加载失败时显示错误

- [ ] **周期切换正常工作**
  - [ ] 点击"今日"按钮，加载今日数据
  - [ ] 点击"本周"按钮，加载本周数据
  - [ ] 点击"本月"按钮，加载本月数据
  - [ ] 点击"本年"按钮，加载本年数据

---

## 📊 数据验证 (Data Validation)

### 数据完整性
- [ ] **Dashboard 数据完整**
  - [ ] summary 对象存在且包含所有必填字段
  - [ ] quality 对象存在且包含所有必填字段
  - [ ] performance 对象存在且包含所有必填字段
  - [ ] trends 对象存在且包含所有必填字段
  - [ ] alerts 数组存在（可以为空）

- [ ] **数据类型正确**
  - [ ] 数值字段确实是数字类型
  - [ ] 字符串字段确实是字符串类型
  - [ ] 日期字段是 ISO 8601 格式字符串
  - [ ] 数组字段确实是数组类型

### 数据准确性
- [ ] **统计数据合理**
  - [ ] totalChecks ≥ 0
  - [ ] totalViolations ≥ 0
  - [ ] totalViolations ≤ totalChecks
  - [ ] 百分比在 0-1 范围内
  - [ ] 时间戳是有效日期

- [ ] **趋势数据正确**
  - [ ] 趋势方向符合预期（up/down/stable）
  - [ ] 趋势百分比合理（不会是 1000%）
  - [ ] 时间序列数据按时间排序

- [ ] **告警数据有效**
  - [ ] 告警严重程度在有效范围（critical/high/medium/low）
  - [ ] 告警描述非空
  - [ ] 告警时间戳有效

---

## 🎨 UI 数据渲染 (UI Rendering)

### StatCard 组件
- [ ] **显示正确的数值**
  - [ ] 总检查次数显示正确（如 1.2K）
  - [ ] 违规次数显示正确
  - [ ] 平均检查时间显示正确（如 245ms）
  - [ ] 今日复盘数显示正确

- [ ] **趋势指示器正确**
  - [ ] 正趋势显示绿色 ↑
  - [ ] 负趋势显示红色 ↓
  - [ ] 零趋势显示灰色 ➖
  - [ ] 趋势百分比格式正确（如 5.2%）

### TrendChart 组件
- [ ] **图表渲染正常**
  - [ ] 违规趋势图显示
  - [ ] 性能趋势图显示
  - [ ] 图表数据点正确
  - [ ] 图表时间轴正确

- [ ] **交互正常**
  - [ ] 悬停显示 tooltip
  - [ ] tooltip 数据正确
  - [ ] 图表响应式调整

### EventStream 组件
- [ ] **WebSocket 连接正常**（见下一节）

---

## 🔌 WebSocket 集成 (WebSocket Integration)

### WebSocket 连接
- [ ] **连接建立成功**
  ```javascript
  const ws = new WebSocket('ws://localhost:3000/ws');
  ws.onopen = () => console.log('Connected');
  ws.onerror = (error) => console.error('Error:', error);
  ```

  **验证：**
  - [ ] 连接状态显示"已连接"（绿点闪烁）
  - [ ] 无连接错误
  - [ ] 浏览器控制台无 WebSocket 错误

- [ ] **消息接收正常**
  ```javascript
  ws.onmessage = (event) => {
    const data = JSON.parse(event.data);
    console.log('Received:', data);
  };
  ```

  **验证：**
  - [ ] 能够接收后端推送的消息
  - [ ] 消息格式符合预期
  - [ ] 消息类型正确（check/violation/retro/info）

### 实时事件显示
- [ ] **事件正确显示**
  - [ ] 新事件自动添加到列表顶部
  - [ ] 事件图标正确（✅ ⚠️ 🔄 ℹ️）
  - [ ] 事件颜色正确（绿/红/蓝/灰）
  - [ ] 事件时间戳正确格式化

- [ ] **事件列表管理**
  - [ ] 最多显示 10 条事件
  - [ ] 旧事件自动移除
  - [ ] 列表自动滚动到最新

### 连接管理
- [ ] **组件卸载时断开连接**
  ```typescript
  useEffect(() => {
    const ws = new WebSocket(WS_URL);
    return () => {
      ws.close();
    };
  }, []);
  ```

- [ ] **错误处理**
  - [ ] 连接失败时显示"未连接"
  - [ ] 连接断开后尝试重连（可选）
  - [ ] 用户可以手动重连

---

## ⚠️ 错误场景测试 (Error Scenarios)

### 后端不可用
- [ ] **后端服务器关闭**
  ```bash
  # 停止后端服务器
  # 观察前端行为
  ```

  **期望行为：**
  - [ ] 显示"加载失败"错误消息
  - [ ] 提供"重试"按钮
  - [ ] 不会白屏或崩溃

### 网络错误
- [ ] **模拟网络延迟**
  - 使用 Chrome DevTools → Network → Throttling
  - [ ] 加载状态显示正常
  - [ ] 超时后显示错误（如有超时设置）

- [ ] **模拟网络中断**
  - [ ] 显示网络错误消息
  - [ ] 提供重试选项

### API 错误响应
- [ ] **404 Not Found**
  - 请求不存在的端点
  - [ ] 显示"接口不存在"错误

- [ ] **500 Internal Server Error**
  - 触发后端错误
  - [ ] 显示"服务器错误"消息

- [ ] **401 Unauthorized**（如有认证）
  - 使用无效 token
  - [ ] 重定向到登录页
  - [ ] 显示"请重新登录"

### 数据异常
- [ ] **空数据**
  - 后端返回空数组或 null
  - [ ] 显示"暂无数据"提示
  - [ ] 不会崩溃

- [ ] **格式错误**
  - 后端返回格式不符合预期
  - [ ] 捕获错误
  - [ ] 显示友好错误消息

---

## 🚀 性能测试 (Performance Testing)

### API 响应时间
- [ ] **Dashboard API**
  - 响应时间：___ ms
  - 目标：<500ms
  - 达成：✅ / ❌

- [ ] **其他 API**
  - Usage API：___ ms
  - Quality API：___ ms
  - Performance API：___ ms

### 前端渲染性能
- [ ] **首次加载**
  - FCP (First Contentful Paint)：___ ms
  - LCP (Largest Contentful Paint)：___ ms
  - TTI (Time to Interactive)：___ ms

- [ ] **数据更新**
  - 周期切换响应时间：___ ms
  - 图表重渲染时间：___ ms

### WebSocket 性能
- [ ] **消息延迟**
  - 后端发送 → 前端接收：___ ms
  - 目标：<100ms

- [ ] **消息吞吐量**
  - 每秒处理消息数：___ msg/s
  - 无消息丢失

---

## 📱 跨浏览器测试 (Cross-Browser Testing)

### Chrome
- [ ] API 调用正常
- [ ] WebSocket 连接正常
- [ ] UI 渲染正确

### Firefox
- [ ] API 调用正常
- [ ] WebSocket 连接正常
- [ ] UI 渲染正确

### Safari（如可用）
- [ ] API 调用正常
- [ ] WebSocket 连接正常
- [ ] UI 渲染正确

### Edge
- [ ] API 调用正常
- [ ] WebSocket 连接正常
- [ ] UI 渲染正确

---

## 📝 文档更新 (Documentation)

### API 文档
- [ ] **端点文档完整**
  - [ ] 请求格式说明
  - [ ] 响应格式说明
  - [ ] 错误代码说明
  - [ ] 示例代码

### 前端文档
- [ ] **集成指南**
  - [ ] 如何启动后端
  - [ ] 如何配置代理
  - [ ] 如何调用 API
  - [ ] 如何处理错误

### 故障排查
- [ ] **常见问题文档**
  - [ ] CORS 错误解决
  - [ ] 连接超时解决
  - [ ] 数据格式错误解决

---

## ✅ 最终验证 (Final Verification)

### 功能完整性
- [ ] 所有计划的 API 端点已集成
- [ ] 所有 UI 组件正常工作
- [ ] 所有用户交互流程通畅

### 代码质量
- [ ] 无 TypeScript 错误
- [ ] 无 ESLint 警告
- [ ] 代码已 code review

### 测试覆盖
- [ ] API 调用有单元测试
- [ ] 组件集成有测试
- [ ] 关键流程有 E2E 测试（可选）

### 部署准备
- [ ] 生产环境配置准备
- [ ] 环境变量文档
- [ ] 部署检查清单（见 deployment.md）

---

## 📊 集成测试报告

### 测试摘要
- **开始时间：** ___
- **结束时间：** ___
- **测试人员：** ___
- **环境：** 开发/测试/生产

### 测试结果
- **总检查项：** ___ / ___
- **通过项：** ___
- **失败项：** ___
- **阻塞问题：** ___

### 发现的问题
| 问题 ID | 描述 | 严重程度 | 状态 |
|---------|------|----------|------|
| 1 | ___ | P0/P1/P2 | Open/Fixed |
| 2 | ___ | P0/P1/P2 | Open/Fixed |

### 后续行动
- [ ] 修复阻塞问题
- [ ] 优化性能瓶颈
- [ ] 完善错误处理
- [ ] 更新文档

---

## 🔗 相关资源

- [API 文档](../../../api/analytics-api.md)
- [前端开发 SOP](../sops/frontend-development.md)
- [部署检查清单](./deployment.md)
- [测试检查清单](./testing.md)

---

**版本历史：**
- v1.0.0 (2026-02-07): 初始版本

**维护者：** Full-stack Team
**审核者：** Tech Lead
**下次更新：** 2026-03-07
