# Save exact versions in package.json
save-exact=true

# Use package-lock for consistency
package-lock=true

# Increase timeout for slow networks
fetch-timeout=60000

# Log level
loglevel=warn

# Fund messages
fund=false

# Audit
audit=true
audit-level=moderate
